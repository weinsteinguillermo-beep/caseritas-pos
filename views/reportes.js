"use strict";

window.CaseritasViews = window.CaseritasViews || {};

window.CaseritasViews.Reportes = {
  render() {
    return `
      <section class="view-placeholder" aria-labelledby="reportes-title">
        <p class="eyebrow">Análisis</p>
        <h2 id="reportes-title">Reportes</h2>
        <p>Vista preparada para ventas, productos más vendidos, stock y resultados diarios.</p>
      </section>
    `;
  },
  init() {},
  destroy() {},
};
